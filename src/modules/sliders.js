const sliders = () => {
  const benefitsCarousel = document.querySelector('.benefits-wrap');
  const benefitSlides = benefitsCarousel.querySelectorAll('.benefits__item');



  if (window.innerWidth < 576){

  }
}

export default sliders;